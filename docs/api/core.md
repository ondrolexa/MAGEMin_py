# Core API

The `MAGEMin` class, the `Point` type used by `multi_point_minimization`, and the parallel
multi-point helper itself.

::: magemin.core.MAGEMin

::: magemin.core.Point

::: magemin.core.multi_point_minimization
