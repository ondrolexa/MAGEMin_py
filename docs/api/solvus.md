# Solvus disambiguation

Ported from MAGEMin_C.jl's `name_solvus.jl` (`get_mineral_name`/`get_ss_from_mineral`). Pure functions,
no C dependency -- usable independently of a `MAGEMin` instance.

::: magemin._solvus.solvus_name

::: magemin._solvus.base_phase_name
