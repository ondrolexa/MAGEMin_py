# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# uv lockfile -- this is a library, so consumers resolve their own versions; not
# committed to avoid spurious diffs/merge conflicts on every dependency bump.
uv.lock

# Tooling caches
.pytest_cache/
.ruff_cache/

# Built docs site
/site/

# Build artifacts from scripts/build_lib.sh.
/src/magemin/magemin_ext/*.o
